# native_keyboard_accessory example

Demonstrates the bar on four field types and lets you switch
`KeyboardAccessoryScope` at runtime to watch it appear and disappear.

```sh
flutter run
```

iOS only — on other platforms the fields work normally and no bar appears.
